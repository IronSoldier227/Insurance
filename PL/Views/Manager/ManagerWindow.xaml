<Window x:Class="PL.ManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PL"
        Title="Панель менеджера" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <!-- Навигационное меню -->
            <ColumnDefinition Width="*"/>
            <!-- Контент (Frame) -->
        </Grid.ColumnDefinitions>

        <!-- Навигационное меню -->
        <StackPanel Grid.Column="0" Background="{StaticResource HeaderBackgroundBrush}" Margin="0,0,5,0">
            <TextBlock Text="Меню" Style="{StaticResource CustomHeaderTextStyle}" Margin="10,10,10,10"/>
            <Button Content="Отчёт по выплатам" Command="{Binding NavigateToReportsCommand}" Style="{StaticResource CustomButtonStyle}" Margin="10,5"/>
            <Button Content="Доходы от полисов" Command="{Binding NavigateToAnnualRevenueReportCommand}" Style="{StaticResource CustomButtonStyle}" Margin="10,5"/>
            <Button Content="Рассмотреть случаи" Command="{Binding NavigateToApproveClaimsCommand}" Style="{StaticResource CustomButtonStyle}" Margin="10,5"/>
            <Button Content="Выход" Command="{Binding LogoutCommand}" Style="{StaticResource CustomButtonStyle}" Margin="10,5"/>
        </StackPanel>

        <!-- Контент -->
        <Frame x:Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden" Margin="5,0,0,0" Background="{StaticResource BackgroundBrush}"/>
    </Grid>
</Window>