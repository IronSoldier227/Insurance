<!-- PL/Views/PaymentsWindow.xaml -->
<Window x:Class="PL.PaymentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Мои выплаты" Height="500" Width="1100" 
    WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="Назад" Command="{Binding GoBackCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Обновить" Command="{Binding LoadPaymentsCommand}" Width="80" Margin="0,0,10,0"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,10" TextWrapping="Wrap" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <DataGrid Grid.Row="2"
                 ItemsSource="{Binding Payments}"
                 AutoGenerateColumns="False"
                 CanUserAddRows="False"
                 CanUserDeleteRows="False"
                 IsReadOnly="True"
                 Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата заявки" Binding="{Binding ClaimDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Полис" Binding="{Binding PolicyNumber}" Width="120"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat={}{0:N2} ₽}" Width="100"/>
                <DataGridTextColumn Header="Дата выплаты" Binding="{Binding PaymentDate, StringFormat=dd.MM.yyyy HH:mm}" Width="120"/>
                <DataGridTextColumn Header="Утверждено менеджером" Binding="{Binding AuthorizedByManagerName}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>